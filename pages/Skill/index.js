import { Collections, Footer } from "@/components";
import React from "react";

const Skill = () => {
  return (
    <div className={`max-w-[1320px] mx-auto px-5 md:px-10 mt-60 `}>
      <Collections />
      <Footer />
    </div>
  );
};

export default Skill;
